/*
 * Your given an array of numbers. Find a unique pair of numbers that sum up
 * to integer t. As order is dropped pairs (2,3) and (3,2) are considered 
 * unique. Only combination of numbers in the pair is considered unique.
 *
 * Answer: Sort the array and start looking from firts + last element. 
 * Search algorithm ibelow has O(n) complexity.
 */
